# 工作日志 · 2026-07-22 · Archer

> 项目：`/Users/mac/Developer/archer` · 分支 `main`  
> 落盘：Grok · 与 git 交叉核对时刻见文末  
> 关联：[`../TODO.md`](../TODO.md) · [`../claude-handoffs/2026-07-22-bridge-console-to-hermes.md`](../claude-handoffs/2026-07-22-bridge-console-to-hermes.md) · 个人月志 `~/Documents/Notes/日记/2026年07月工作日志.md`

---

## 一句话

多 agent 座舱主线落地并入库：**@label 桥 + Agent Bridge 指挥台 + handoff 写路径 + parallel 标记/Dashboard + Observability 岛**；**Usage 窗产品取消**；全量测 **618/0**；主体已进 `main`（相对 `origin/main` 仍可能 ahead 未推文档/尾 commit，以 `git status` 为准）。

---

## 时间线

| 阶段 | 内容 | 结果 |
|------|------|------|
| 诊断 / 产品 | Bridge 开 Hermes tab 缺口；多 agent GUI 形态 | **否决中央 chat**；采纳 **`@label` 寻址** |
| 写路径 | `openAgentTab` + Bridge `handoff`/`open`/`agents` + CLI + GUI Hand off brief | 代码 + 单测 |
| @label | PaneRegistry 全 non-shell；drivenBy；Tab 徽章；CLI strip `@` | 路线跑通 |
| 指挥台 | Console / Action / ActivityBar / RosterStrip / Launcher；⌘⇧B；⌘P | 对齐 design `bridge.html` |
| Skeptic | storeProvider 必填；↗ 带 store；0 bare `show()`；无 store 不 `sync(nil)` | 测绿后入库 |
| 拖放 / parallel | chrome drop 开 tab；`parallelTaskGroupId` + 侧栏 `∥` | 标记 → Dashboard |
| 新岛 | Observability + Parallel Group Dashboard | ⌘⇧I / ⌘⇧G / palette / window menu |
| 产品 | **Usage 窗不恢复**；design PR4 / usage-dashboard 取消 | 文档 SUPERSEDED |
| 文档 / 交接 | worklog · TODO · Hermes handoff | 本文件 |
| 发布 | 系列 commit 进 `main`；release/DMG 相关见当日后续 commit | 见下方 SHA |

---

## 关键提交（`4358156` → HEAD 一带）

```text
1fd5142 feat(bridge): Agent Bridge console, @label handoff, parallel group tags
8c03482 feat(multiagent): add observability and parallel-group dashboard
7c53515 feat(multiagent): observability command palette and activation hook
98dc330 feat(multiagent): observability palette activation and window menu
a01ac21 feat(multiagent): add parallel-group dashboard controller and view cleanup
870aece test(multiagent): add ParallelGroupDashboardIndexTests
1cae097 feat(multiagent): add parallel-groups palette, window menu and activation
c6fd45a docs(TODO): mark parallel-group dashboard as shipped
7574548 fix(multiagent): observability filter state + parallel group members enumeration
101266a docs(TODO): mark observability timeline as shipped
a2e8126 feat(multiagent): bridge console, sidebar handoff, memory dedup, observability, dashboard
0e72271 chore(usage): SessionLiveUsage tweaks
d14a552 feat(multiagent): observability token-delta scale + bridge log feed
44ec819 docs(worklog): update 2026-07-22 release/push/dmg notes
```

（完整列表以 `git log 4358156..HEAD --oneline` 为准。）

---

## 验证

```text
swift test
→ 618 tests / 0 failures   # 会话内终验口径

# 早期 Skeptic 子集
swift test --filter 'BridgeActionTests|BridgeHandoffTests'
→ 13 / 0
```

### Skeptic 三项（行为不变量 · 勿回退）

1. `BridgeActivityBar` 持 `store`；↗ → `BridgeConsoleLauncher.open(store:)`
2. `ContentView`：`BridgeActivityBar(store: store)`
3. `LogPanelWindowController.show(storeProvider:)` **必填**；禁止 bare `show()`

---

## 产品决策

| 决策 | 说明 |
|------|------|
| 桥 = `@label` | 不是中央 multi-agent 聊天室 |
| Usage 窗 | **取消、不恢复**；`Usage/` 采集库可保留给 Sessions |
| design PR4 | CANCELLED |
| 指挥台 | Agent Bridge ⌘⇧B；主窗 roster + activity bar |
| Observability | ⌘⇧I |
| Parallel groups | ⌘⇧G + Dashboard 汇总 |

---

## 交付物索引

| 区域 | 路径 / 能力 |
|------|-------------|
| Bridge 指挥台 | `Sources/ArcherKit/Bridge/{BridgeConsole*,BridgeAction,BridgeActivityBar,AgentRosterStrip,BridgeConsoleLauncher,PaneRegistry,BridgeServer,…}` |
| Handoff | `WorkspaceStore.openAgentTab` · CLI `Sources/ArcherBridge` · Sidebar Hand off |
| Parallel | `Workspace.parallelTaskGroupId` · Dashboard 控制器/视图 · 侧栏 `∥` |
| Observability | Observability 窗 / token-delta / BridgeEventLog feed |
| Docs | `docs/TODO.md` · `docs/design-system-refactor-plan.md` · handoff · 本日志 |

---

## 未完成 / 下一棒

见 [`../TODO.md`](../TODO.md)。摘要：

- [ ] GUI 手测：⌘⇧B / handoff live / drop / `∥` / ⌘⇧I / ⌘⇧G
- [ ] 确认 `git push` 是否把 `origin/main` 追平（若仍 ahead）
- [ ] Skills 视觉对齐 design-system（**无** Usage）
- [ ] workspace-template / agent-interop / EdgeGlow P3（用户点名再开）

---

## 明确不做

- 恢复 Usage 窗 / usage-dashboard  
- 中央 multi-agent chat room  
- 改 Theme glass 三全局默认  

---

## 元

- 个人笔记副本：`~/Documents/Notes/日记/2026年07月工作日志.md` → 节 `## 2026-07-22`  
- STATE 指针：`STATE.md` §5  

---

## 旁线 · 设计史双轨（非 Archer 主线）

- 路径：`/Users/mac/Desktop/设计史双轨/`
- 日志：`SESSION-LOG-2026-07-22.md`
- Handoff → Hermes：`HERMES-HANDOFF.md`（判定+执行 H0–H2）
- 门禁：`python3 scripts/test_integrity_gate.py` PASS（21 章 · examTf=22 · 关键乱码 0）
- **勿**与 Archer Bridge commit 混提

