# 工作日志 · 2026-07-22 · Archer

> 项目：`/Users/mac/Developer/archer` · 分支 `main` · **已推送 origin/main**  
> 关联：[`../claude-handoffs/2026-07-22-bridge-console-to-hermes.md`](../claude-handoffs/2026-07-22-bridge-console-to-hermes.md) · [`../TODO.md`](../TODO.md)

---

## 一句话

把 **@label 桥 + Agent Bridge 指挥台 + handoff 写路径 + parallel 标记 + Observability/Dashboard** 推到「代码齐、618/0 测绿、已推送」；**Usage 窗产品取消**。

---

## 时间线（会话内）

| 阶段 | 做了什么 | 结果 |
|------|----------|------|
| 诊断 / 路线 | Bridge 开 Hermes tab 缺口；多 agent GUI 讨论 | 否决中央 chat；采纳 **@ 寻址** |
| 写路径 | `openAgentTab` + Bridge `handoff`/`open`/`agents` + CLI + GUI Hand off brief | 代码 + 单测 |
| @label | PaneRegistry 全 non-shell；drivenBy；Tab 徽章；CLI strip `@` | 路线跑通 |
| 指挥台 | `BridgeConsoleView` / Action / ActivityBar / RosterStrip / Launcher；⌘⇧B；⌘P | 对齐 design bridge.html |
| Skeptic / CLI | storeProvider 必填；↗ 必带 store；无 bare `show()`；无 store 不 `sync(nil)` | **618/0** 全量绿 |
| 拖放 / parallel | chrome drop 开 tab；`parallelTaskGroupId` + 侧栏 `∥` + Dashboard | 已落地 |
| 新岛 | `ObservabilityView` / `WindowController`；token-delta scale；BridgeEventLog feed | ⌘⇧I / ⌘⇧G / palette |
| 产品判定 | **Usage 窗不恢复**；design PR4 取消 | 确认 |
| 发布 | `git push` + 本地 DMG + 远端 GitHub release | 执行中 |

---

## 今日交付

- `1fd5142` feat(bridge): Agent Bridge console, @label handoff, parallel group tags
- `8c03482` feat(multiagent): add observability and parallel-group dashboard
- `7c53515` feat(multiagent): observability command palette and activation hook
- `98dc330` feat(multiagent): observability palette activation and window menu
- `1cae097` feat(multiagent): add parallel-groups palette, window menu and activation
- `c6fd45a` docs(TODO): mark parallel-group dashboard as shipped
- `870aece` test(multiagent): add ParallelGroupDashboardIndexTests
- `a01ac21` feat(multiagent): add parallel-group dashboard controller and view cleanup
- `7574548` fix(multiagent): observability filter state + parallel group members enumeration
- `101266a` docs(TODO): mark observability timeline as shipped
- `a2e8126` feat(multiagent): bridge console, sidebar handoff, memory dedup, observability, dashboard
- `0e72271` chore(usage): SessionLiveUsage tweaks
- `d14a552` feat(multiagent): observability token-delta scale + bridge log feed

---

## 验证

```text
swift test
→ 618 tests / 0 failures
```

---

## 产品决策

| 决策 | 说明 |
|------|------|
| 桥 = `@label` | 不是中央 multi-agent 聊天室 |
| Usage 窗 | **不需要、不恢复**；采集库 `Usage/` 可保留 |
| design PR4 | CANCELLED（usage-dashboard） |
| 指挥台 | Window → Agent Bridge ⌘⇧B；主窗 roster + activity bar |
| Observability | ⌘⇧I；Dashboard ⌘⇧G；Command Palette 双入口 |

---

## 未完成 / 风险

- GUI 手测（⌘⇧B / handoff live / drop / `∥`）需本地启动 Archer
- Observability 可继续接入更多数据源（当前 tab + bridge log）
- 其余见 [`../TODO.md`](../TODO.md)

---

## 索引

| 文档 | 用途 |
|------|------|
| `STATE.md` §5 | 会话指针 |
| `docs/TODO.md` | 活 todo |
| `docs/claude-handoffs/2026-07-22-…` | Hermes 可执行 handoff |
| `docs/BACKLOG.md` | 产品 backlog |
| `docs/design-system-refactor-plan.md` | 视觉 PR 路线（无 Usage） |
