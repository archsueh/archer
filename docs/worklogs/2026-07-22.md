# 工作日志 · 2026-07-22 · Archer

> 项目：`/Users/mac/Developer/archer` · 分支 `main` · **WIP 未 commit**  
> 执行 agent：Grok · 交接对象：Hermes  
> 关联：[`../claude-handoffs/2026-07-22-bridge-console-to-hermes.md`](../claude-handoffs/2026-07-22-bridge-console-to-hermes.md) · [`../TODO.md`](../TODO.md)

---

## 一句话

把 **@label 桥 + Agent Bridge 指挥台 + handoff 写路径 + parallel 标记** 推到「代码齐、Skeptic 测绿」；**Usage 窗产品取消**；工作树未提交、已写 Hermes handoff。

---

## 时间线（会话内）

| 阶段 | 做了什么 | 结果 |
|------|----------|------|
| 诊断 / 路线 | Bridge 开 Hermes tab 缺口；多 agent GUI 讨论 | 否决中央 chat；采纳 **@ 寻址** |
| 写路径 | `openAgentTab` + Bridge `handoff`/`open`/`agents` + CLI + GUI Hand off brief | 代码 + 单测 |
| @label | PaneRegistry 全 non-shell；drivenBy；Tab 徽章；CLI strip `@` | 路线跑通 |
| 指挥台 | `BridgeConsoleView` / Action / ActivityBar / RosterStrip / Launcher；⌘⇧B；⌘P | 对齐 design bridge.html |
| Skeptic | storeProvider 必填；↗ 必带 store；无 bare `show()`；无 store 不 `sync(nil)` | **13/0** BridgeAction+Handoff |
| 拖放 / parallel | chrome drop 开 tab；`parallelTaskGroupId` + 侧栏 `∥` | 起步，无结果 Dashboard |
| 产品判定 | **删除 / 不恢复 Usage 窗**；docs SUPERSEDED + design PR4 取消 | 确认 |
| 设计缺口 | vs `archer-design-system` 矩阵 / refactor plan | 文档；Usage 标取消 |
| 收尾 | Hermes handoff 落盘；STATE §5 指针 | 可交接 |

---

## 今日交付（代码 / 文档）

### 新文件（untracked）

**Bridge**

- `Sources/ArcherKit/Bridge/AgentRosterStrip.swift`
- `Sources/ArcherKit/Bridge/BridgeAction.swift`
- `Sources/ArcherKit/Bridge/BridgeActivityBar.swift`
- `Sources/ArcherKit/Bridge/BridgeConsoleLauncher.swift`
- `Sources/ArcherKit/Bridge/BridgeConsoleView.swift`
- `Tests/ArcherKitTests/BridgeActionTests.swift`
- `Tests/ArcherKitTests/BridgeHandoffTests.swift`
- `Tests/ArcherKitTests/PaneRegistryTests.swift`

**Docs**

- `docs/bridge-handoff-project.md`
- `docs/design-system-refactor-plan.md`
- `docs/design-tokens-matrix.md`
- `docs/claude-handoffs/2026-07-22-bridge-console-to-hermes.md`
- `docs/worklogs/2026-07-22.md`（本文件）
- `docs/TODO.md`

**另有（勿与 Bridge 混提）**

- `MemoryDedup` / `MemoryEntityGraph` / `MemorySemanticSearch` + 对应 Tests
- `Sessions/ExecutionRouter.swift`（确认归属后再提）

### 主要改动（modified）

- Bridge：`BridgeServer` · `PaneRegistry` · `LogPanelView`
- App：`AppDelegate` · `CommandPalette` · `ContentView`
- Sessions：`WorkspaceStore` · `Session` · `Workspace` · TabBar*
- Sidebar：Hand off brief · parallel chip 等
- Cockpit：Usage 入口保持删除
- `STATE.md` · `docs/BACKLOG.md` · `docs/usage-tokenscope-plan.md`

### 验证证据

```text
swift test --filter 'BridgeActionTests|BridgeHandoffTests'
→ 13 XCT / 0 failures
  · testActivityBarOpenPathKeepsStoreAndRegistry
  · testSyncNilWipesRegistryButStorePathPreservesLabels
  · testBridgeActivityBarRequiresStoreProperty
  · handoff / type / agents 相关
```

Skeptic 现树：

1. `BridgeActivityBar.let store` + ↗ → `BridgeConsoleLauncher.open(store:)`
2. `ContentView` → `BridgeActivityBar(store: store)`
3. `LogPanelWindowController.show(storeProvider:)` 必填；0 bare `show()`

---

## 产品决策（今日拍板）

| 决策 | 说明 |
|------|------|
| 桥 = `@label` | 不是中央 multi-agent 聊天室 |
| Usage 窗 | **不需要、不恢复**；采集库 `Usage/` 可保留 |
| design PR4 | CANCELLED（usage-dashboard） |
| 指挥台 | Window → Agent Bridge ⌘⇧B；主窗 roster + activity bar |

---

## 未完成 / 风险

| 项 | 说明 |
|----|------|
| **未 commit** | ~40 文件；含 Memory* 混杂 |
| **未手测** | ⌘⇧B / handoff live / drop / `∥` |
| parallel 聚合 | 仅 groupId + chip，无结果 Dashboard |
| Memory* | 另一线 WIP，禁止与 Bridge 同 commit |
| pre-commit | 可能 `git add -u` → stage 前先 stash Memory* |

---

## 明日 / 接棒起点

见 [`../TODO.md`](../TODO.md)。  
启动 Hermes：

```bash
cd /Users/mac/Developer/archer
# 读 docs/claude-handoffs/2026-07-22-bridge-console-to-hermes.md
# 执行 T0–T4；Repo wins if conflict
```

---

## 索引

| 文档 | 用途 |
|------|------|
| `STATE.md` §5 | 会话指针 |
| `docs/TODO.md` | 活 todo |
| `docs/claude-handoffs/2026-07-22-…` | Hermes 可执行 handoff |
| `docs/BACKLOG.md` | 产品 backlog |
| `docs/design-system-refactor-plan.md` | 视觉 PR 路线（无 Usage） |
