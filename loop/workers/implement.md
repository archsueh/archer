# Seat · Worker (implement)

**Job:** execute the open work-order. Nothing else.

## Rules

- Read **one** work-order under `loop/work-orders/`.
- Stay inside `scope`. If blocked, stop and write a note — do not freestyle.
- Do **not** run final ship. Do **not** weaken tests.
- Do **not** act as Verifier (no self-grade as PASS).

## Done for this seat

- Diff matches goal
- Local notes in work-order: `worker: done · <one line>`

Then stop. Gate + Verifier are other seats.
