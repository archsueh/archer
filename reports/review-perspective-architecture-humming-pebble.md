{"findings":[{"finding_id":"ARCH-001","severity":"high","category":"bug","title":"BridgeServer.store pinned to windowControllers.first — multi-window bridge broken","evidence":{"file":"Sources/ArcherKit/Bridge/BridgeServer.swift","line":27,"symbol":"BridgeServer.store","snippet":"weak var store: WorkspaceStore?\n// AppDelegate.swift:119\nbridgeServer.store = windowControllers.first?.store\n// BridgeServer.swift:102 — every bridge command uses this single store\nPaneRegistry.shared.sync(workspace: store?.active)"},"recommendation":"Replace the single stored reference with a closure that resolves the active store at command time: `var storeProvider: (() -> WorkspaceStore?)? `. In `handle(_:)`, call `storeProvider?()` instead of `store`. Wire it in AppDelegate to `{ [weak self] in self?.activeController?.store }`. This mirrors how hookServer already broadcasts across all windows rather than pinning to window[0].","confidence":0.95,"fingerprint":"arch-001-bridgeserver-store-stale-single-window"},{"finding_id":"ARCH-002","severity":"high","category":"performance","title":"BridgeServer.acceptOne() calls blocking read() on the main queue — any slow client freezes the UI","evidence":{"file":"Sources/ArcherKit/Bridge/BridgeServer.swift","line":86,"symbol":"BridgeServer.acceptOne","snippet":"let n = buf.withUnsafeMutableBufferPointer { read(clientFd, $0.baseAddress, $0.count) }\n// This is a synchronous blocking syscall on DispatchQueue.main (the DispatchSourceRead handler queue)"},"recommendation":"After accept(), hand the clientFd to a background DispatchQueue for the read()/write() cycle, then hop back to MainActor only to call handle(_:). Set SO_RCVTIMEO on the accepted socket (e.g. 2s) before the read so a hung client cannot block indefinitely even on the background queue.","confidence":0.92,"fingerprint":"arch-002-bridgeserver-blocking-read-main-thread"},{"finding_id":"ARCH-003","severity":"medium","category":"bug","title":"reconcileWorktrees can silently remove the active workspace, leaving activeWorkspaceId dangling","evidence":{"file":"Sources/ArcherKit/Sessions/WorkspaceStore.swift","line":487,"symbol":"WorkspaceStore.reconcileWorktrees","snippet":"// After async gap (git subprocess fan-out):\nfor result in results {\n    guard let source = workspaces.first(where: { $0.id == result.sourceId }) else { continue }\n    reconcile(source: source, sourceRoot: result.repoRoot, diskWorktrees: result.infos)\n}\n// reconcile() removes zombie worktree sidebar workspaces from self.workspaces\n// If activeWorkspaceId pointed to one, store.active becomes nil with no recovery"},"recommendation":"After each `reconcile()` call, check whether `activeWorkspaceId` is still present in `workspaces`. If not, auto-select the nearest surviving workspace (e.g., the source workspace that owned the removed worktree). This should be a targeted fix inside `reconcile()` or immediately after its call site, not a general nil-guard in `active`.","confidence":0.80,"fingerprint":"arch-003-reconcile-dangling-activeworkspaceid"}]}
